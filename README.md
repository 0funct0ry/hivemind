# hivemind

hivemind is a minimal self-hosted team chat server. 
Drop it on a box, run `hivemind serve`, open a browser.

## Quickstart

```bash
make build
./bin/hivemind serve
```

Then open the URL printed on the first run to create the admin account.

## Status

Pre-alpha. Under active development toward v0.1.0.
